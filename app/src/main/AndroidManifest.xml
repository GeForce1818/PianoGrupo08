<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.PianoGrupo08">

        <!-- Piano Tradicional como pantalla principal -->
        <activity
            android:name=".PianoTradicionalActivity"
            android:exported="true"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Piano de Instrumentos -->
        <activity
            android:name=".PianoInstrumentosActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

        <!-- Piano Infantil (Animales de la Selva) -->
        <activity
            android:name=".PianoAnimales"
            android:exported="false"
            android:screenOrientation="landscape" />

        <!-- Pantalla Acerca de -->
        <activity
            android:name=".AcercaDeActivity"
            android:exported="false"
            android:screenOrientation="landscape" />

    </application>

</manifest>
